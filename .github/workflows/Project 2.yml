name: Project 2 | CI/CD Pipeline

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      mongodb:
        image: mongo:6.0
        ports:
          - 27017:27017

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"
          cache: "npm"
          cache-dependency-path: ./Project 2

      - name: Install dependencies
        run: npm ci
        working-directory: ./Project 2

      - name: Run tests
        run: npm run test:coverage
        env:
          MONGODB_URL: mongodb://localhost:27017
        working-directory: ./Project 2

      - name: Upload coverage reports
        uses: codecov/codecov-action@v3
        with:
          file: ./Project 2/coverage/lcov.info

  build-and-test-docker:
    runs-on: ubuntu-latest
    needs: test

    steps:
      - uses: actions/checkout@v3

      - name: Build Docker image
        run: docker build -t fleet-tracking ./Project 2

      - name: Run tests in Docker
        run: |
          docker run --rm -e MONGODB_URL=mongodb://host.docker.internal:27017 fleet-tracking npm run test
        env:
          MONGODB_URL: mongodb://host.docker.internal:27017
